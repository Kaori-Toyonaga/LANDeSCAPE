<h2>User Name</h2>
<p>User_image</p>
<a>✍</a>
<P><span>〇〇</span>  <span>●●</span></p>
<P><span>follower</span>  <span>follow</span></p>
<a><%= link_to "Logout", session_path(current_user.id), method: :delete %></a>
<p>〇〇/48</p>
<p>〇〇 posts</p>

% @posts.each do |post| %>
  <p>
    <span>image %= link_to '🔍', post_path(post.id) %></span>
  </p>
  <p>%= post.spot_name %></p>
  <p>%= post.prefecture %>%= post.address %></p>
% end %>
